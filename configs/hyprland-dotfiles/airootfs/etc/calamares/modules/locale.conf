---
# Locale module config (Calamares 3.4 schema)

region: "America"
zone:   "New_York"

# Override the default /etc/locale.gen path if the distro keeps it elsewhere.
localeGenPath: "/etc/locale.gen"

geoip:
    style:    "json"
    url:      "https://geoip.kde.org/v1/calamares"
    selector: ""
