---
# Fstab module config (Calamares 3.4 schema).
#
# Mount options for /etc/fstab now live in mount.conf — keep them in sync
# there. This file only carries crypttab/tmp settings.

crypttabOptions: luks
