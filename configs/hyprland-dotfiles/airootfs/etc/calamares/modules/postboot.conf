---
# Post-install step: boot splash, GPU autoconfig, initramfs, Limine entries
i18n:
    name: "Configuring boot and graphics"
dontChroot: false
timeout: 3600

script:
    - "/usr/local/bin/post-install-boot"
