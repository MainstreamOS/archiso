# DEBUG (temporary): allow same-UID ptrace so dotfiles-first-login's first-boot
# qs-block probe can strace the quickshell process to name what it blocks on
# during the ~21s first-boot bar-paint delay. REMOVE this file (and strace from
# packages.x86_64, and the strace block in dotfiles-first-login) once diagnosed.
kernel.yama.ptrace_scope = 0
