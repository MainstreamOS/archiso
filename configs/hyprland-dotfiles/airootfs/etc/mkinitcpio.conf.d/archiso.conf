# Explicit MODULES instead of the `kms` hook: kms auto-bundles every GPU's
# firmware, which bakes NVIDIA's GSP blob into the init and inflates it. The
# initramfs is carried twice now, in ISO 9660 and again in the EFI system
# partition systemd-boot reads from, so its size is paid for twice.
# Keeps AMD/Intel early-KMS; NVIDIA loads post-pivot (modprobe.d + udev rule).
# The virtual adapters belong here as much as the real cards do. Without a DRM
# driver at early KMS a guest has nothing to set a mode on, so the default entry
# dies and only the safe-graphics one, which skips KMS, comes up. vmwgfx covers
# VMware and VirtualBox's VMSVGA both, virtio-gpu covers QEMU, KVM and Proxmox,
# and the whole set adds well under a megabyte.
MODULES=(amdgpu i915 radeon vmwgfx vboxvideo virtio-gpu hyperv_drm qxl bochs)
HOOKS=(base udev plymouth keyboard microcode modconf memdisk archiso archiso_loop_mnt archiso_pxe_common archiso_pxe_nbd archiso_pxe_http archiso_pxe_nfs block filesystems)
COMPRESSION="zstd"
COMPRESSION_OPTIONS=(--long -19 -T0)
